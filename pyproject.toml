[project]
name = "b-shravan"
version = "0.1.0"
description = "to upload audio in Telegram"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mutagen>=1.47.0",
    "python-dotenv>=1.2.2",
    "telethon>=1.43.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
